// No formal projects listed on the resume — kept empty so the section stays hidden.
// When you build portfolio-worthy projects later, add them here and the section will appear automatically.
export const projects = [];
export const hasProjects = projects.length > 0;
